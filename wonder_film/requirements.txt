# Python 3.11 (bpy wheels are built for 3.11)
bpy==4.5.4          # Blender as a Python module: Cycles CPU rendering
numpy>=1.26
scipy>=1.11
opencv-python-headless>=4.9
OpenEXR>=3.3
Pillow>=10.0
fonttools>=4.50     # only needed to re-convert the bundled fonts
# system: ffmpeg (libx264 + aac); for headless EGL: libegl1 libegl-mesa0
